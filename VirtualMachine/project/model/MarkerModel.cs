namespace VirtualMachine.project.model
{
    public class MarkerModel : Model
    {
        public MarkerModel(string location, string name) : base(location, name, ModelType.MARKER)
        { }
        
    }
}